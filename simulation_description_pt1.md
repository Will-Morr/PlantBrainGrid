Plant-based evolution simulation. The world should be a great of two-dimensional cells that have various stats a plant. It's an individual that has a primary cell at a fixed location that has the ability to police cells next to any existing cell that is a part of it. There should be leaf cells that can take in sunlight and produce energy should be root cells that can gather water or should be xylem cells that can transmit resources. Resources should retract on a cell by cell basis. All additional cells have a required resource value to place. Resources need to be funneled into the marcell be used there should also be thorn cells that prevent any other plant cells from being placed next to them there should also be big leaves that take large amount of water to maintain and small leaves that do not each plant's brain should be a large block of contiguous memory like in a compiled program. There should be a pointer to the current execution place and each tick of the simulation of these Friends programs should be iterated until they hit a stop point or break a rule. The loop for running a command is first to load The current bite and modulo it with the number of total commands to pick a command to execute. Any information required by that command should be in adjacent memory, updating the current function pointer each time and should be able to place cells. Read the states of cells. Read arbitrary positions on the map relative to themselves and to decide to turn on or off various weaves thorns asylum it should also be able to arbitrarily read and write from its own memory. One of the primary functions plants can do is reproduce. When it does this it should select should be able to filter most criteria but that plant could have and then should select an amount of each resource. Dad wants to pass off to this year. AC not consume with any resources. Seeds can be destroyed by placing cells should also need to select a certain amount of energy to use to launch the seed and that sets the fire and radius of the seed can be watched too another thing selected as part of sexual reproduction should be the recombination method. There should be options to purely use the mother or fathers or be combined randomly or select every other gene. A gene in this context is a byte of memory in the brain. The map should be randomly filled with water values using purlin noise to establish areas with less and more water. The curling noise should be low frequency. The xylem should be simple. It should simply take resources from his neighbors and pass them in a direction there should also be a season system where the amount of light and available varies gradually over time. There should also be a second water-like resource called nutrients, which varies according to a similar purlin noise gradient it should be required in small amounts for everything that large leaves should require large amounts of nutrients. 

The primary simulation code should be written in c++. The tick of the simulation should be able to be emulated entirely in c++ A second layer on top should actually run the ticks and the display through the display. I would like a large 2D visualization with the ability to zoom and drag around. Ideally, it is written in Python, but I'm open to suggestions. The larger simulation should also give the ability to select certain plants. If a plant is selected, I would like to be able to view a real-time output of its brain as it runs. The simulation should also be able to be paused and periodically backs up simulation state so that I can go backwards and forwards and the random seed of the SIM should be saved as part of gamestate so that loading an older state is purely deterministic I would also like an option to gradually form a map of the brain with every possible path that it follows being recorded and visualized in parallel rather than a continuous log dump

The brain should also support forking decision making and go-to statements so that the plant is able to respond to all aspects of the simulation and change its own behavior. Reproduction should also always introduce some random noise into the genes. This random noise should be independent of the existing structure. Call this random noise mutations 

Finally, there should be a cell that starts a fire fire spreads gradually to adjacent tiles over time. Cells are destroyed after a certain number of ticks at which time fire spreads to all adjacent cells. There should be a special type of phloem that takes more nutrients to place but is immune to fire

Please write comprehensive tests for each individual part of the system run them for attempting to launch the whole project
